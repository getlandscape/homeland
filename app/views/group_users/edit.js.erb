(function () {
    target = "<%= escape_javascript(render partial: '/group_users/edit', locals: { group: @group, group_user: @group_user })%>"
	replaceHTML($('#edit-group-user-modal-body'), target);
    $('#edit-group-user-modal').modal('show');
})();