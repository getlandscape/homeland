(function () {
    target = "<%= escape_javascript(render partial: '/group_users/manage', locals: { group: @group, group_user: @group_user, target: params[:target] })%>"
	replaceHTML($('#edit-group-user-modal-body'), target);
    $('#edit-group-user-modal').modal('show');
})();