<% title_tag t('groups.new_group') %>
<div class="card card-lg">
  <div class="card-body">
    <div class="card-title hide-ios">
      <%= t('groups.new_group') %>
    </div>
    <%= render 'form' %>
  </div>
</div>
