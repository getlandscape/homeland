(function () {
    target_one = "<%= escape_javascript(render '/groups/content_form')%>"
	target_two = "<%= escape_javascript(render '/groups/sidebar_for_group_topics', group: @group)%>"

	replaceHTML($('#group-info-content'), target_one);
	replaceHTML($('#group-sidebar-content'), target_two);
})();