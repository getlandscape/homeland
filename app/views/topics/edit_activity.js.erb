(function () {
    target = "<%= escape_javascript(render partial: 'edit_activity', locals: { topic: @topic, user_topic: @user_topic, opt: params[:opt] })%>"
	replaceHTML($('#edit-activiy-modal-body'), target);
    $('#edit-activiy-modal').modal('show');
})();