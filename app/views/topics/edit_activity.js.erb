(function () {
    target = "<%= escape_javascript(render partial: '/topics/edit_activity', locals: { topic: @topic, user_topic: @user_topic })%>"
	replaceHTML($('#edit-activiy-modal-body'), target);
    $('#edit-activiy-modal').modal('show');
})();